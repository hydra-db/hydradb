apiVersion: v2
name: hydradb
description: Stateless distributed graph database backed by SlateDB and object storage
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.27.0-0"
home: https://github.com/hydra-db/hydradb
sources:
  - https://github.com/hydra-db/hydradb
keywords:
  - graph
  - cypher
  - slatedb
  - object-storage
  - graphblas
annotations:
  # Must match LICENSE. This read Apache-2.0 while the repository shipped
  # AGPL-3.0, which Artifact Hub would have published as the chart's licence.
  artifacthub.io/license: AGPL-3.0-only
