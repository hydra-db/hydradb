objectStore:
  aws:
    bucketName: graph-ci

auth:
  token: ci-only-auth-token-with-more-than-32-characters
##
tls:
  public:
    enabled: false

node:
  replicaCount: 3

networkPolicy:
  httpsEgressTo:
    - ipBlock:
        cidr: 10.0.0.0/8
